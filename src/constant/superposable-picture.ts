import { join } from 'path';

export const SUPERPOSABLE_PICTURE_PATH = join(process.cwd(), 'src', 'public', 'superposable-picture');

export const USER_IMAGE_PATH = join(process.cwd(), 'src', 'user-image');
